services:
  - type: web
    name: CourseCertificateRequestSystem
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn certificate_system.wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0