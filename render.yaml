services:
  - type: web
    name: CourseCertificateRequestSystem
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: gunicorn certificate_system.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0